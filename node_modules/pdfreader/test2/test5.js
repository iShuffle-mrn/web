var TESTFILE = "./test2/input4.pdf";

// step 1: print raw items

function printRawItems(callback){
  new PdfReader().parseFileItems(TESTFILE, function(err, item){
    if (err)
      callback(err);
    else if (!item)
      callback();
    else
      console.log(item);
  });
}